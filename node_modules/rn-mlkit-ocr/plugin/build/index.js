"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const config_plugins_1 = require("@expo/config-plugins");
const withMlKitConfig = (config, props = {}) => {
    config = (0, config_plugins_1.withProjectBuildGradle)(config, (gradleConfig) => {
        if (gradleConfig.modResults.language === 'groovy') {
            gradleConfig.modResults.contents = setGradleProperties(gradleConfig.modResults.contents, props);
        }
        return gradleConfig;
    });
    config = (0, config_plugins_1.withPodfile)(config, (podfileConfig) => {
        podfileConfig.modResults.contents = setPodfileProperties(podfileConfig.modResults.contents, props);
        return podfileConfig;
    });
    return config;
};
function setGradleProperties(buildGradle, props) {
    const { ocrModels, ocrUseBundled } = props;
    const models = ocrModels || ['all'];
    const useBundled = ocrUseBundled !== undefined ? ocrUseBundled : false;
    const modelsString = `["${models.join('", "')}"]`;
    const newConfig = `
        // --- RN-MLKIT-OCR CONFIG START ---
        ocrModels = ${modelsString}
        ocrUseBundled = ${useBundled}
        // --- RN-MLKIT-OCR CONFIG END ---
  `;
    if (buildGradle.includes('ocrModels =')) {
        return buildGradle.replace(/ocrModels\s?=\s?\[[\s\S]*?\]\s+ocrUseBundled\s?=\s?(true|false)/, `ocrModels = ${modelsString}\n        ocrUseBundled = ${useBundled}`);
    }
    if (buildGradle.includes('ext {')) {
        return buildGradle.replace('ext {', `ext {${newConfig}`);
    }
    return buildGradle + `\nbuildscript {\n    ext {${newConfig}\n    }\n}\n`;
}
function setPodfileProperties(podfile, props) {
    const { ocrModels } = props;
    let models = [];
    if (!ocrModels || ocrModels.includes('all')) {
        models = ['latin', 'chinese', 'devanagari', 'japanese', 'korean'];
    }
    else {
        models = ocrModels.map((m) => m.charAt(0).toUpperCase() + m.slice(1));
        if (!models.includes('latin')) {
            models.push('latin');
        }
    }
    const variableContent = `$ReactNativeOcrSubspecs = [${models
        .map((m) => `'${m}'`)
        .join(', ')}]`;
    const tag = '# --- RN-MLKIT-OCR CONFIG ---';
    if (podfile.includes(tag)) {
        return podfile.replace(/# --- RN-MLKIT-OCR CONFIG ---[\s\S]*?# --- END RN-MLKIT-OCR CONFIG ---/, `${tag}\n${variableContent}\n# --- END RN-MLKIT-OCR CONFIG ---`);
    }
    return (`${tag}\n${variableContent}\n# --- END RN-MLKIT-OCR CONFIG ---\n\n` +
        podfile);
}
exports.default = withMlKitConfig;
